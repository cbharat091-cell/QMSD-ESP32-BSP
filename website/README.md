# QMS Portfolio (static)

This is a minimal static portfolio site scaffold placed under `website/`.

To preview locally, open `website/index.html` in a browser or use a static server, e.g.:

```bash
python3 -m http.server --directory website 8080
```

Deploy to Vercel by connecting the folder to a Vercel project.
